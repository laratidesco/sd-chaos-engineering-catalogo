apiVersion: v1
kind: Service
metadata:
  name: api-gateway-naive
  labels:
    app: api-gateway-naive
spec:
  type: NodePort
  selector:
    app: api-gateway-naive
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      nodePort: 30081
