apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: api-gateway-naive
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: api-gateway-naive
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
